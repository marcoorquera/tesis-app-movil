import { FiltroProdPipe } from './filtro-prod.pipe';

describe('FiltroProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProdPipe();
    expect(pipe).toBeTruthy();
  });
});
