import { FiltroCategoryPipe } from './filtro-category.pipe';

describe('FiltroCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
