import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.muebleria.global',
  appName: 'Global Muebles',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
